syntax = "proto3";

package inventory;
option go_package = "./gen;inventory";

service InventoryService {
  // Получить остаток товара по его ID
  rpc GetStock (StockRequest) returns (StockResponse);
}

message StockRequest {
  string product_id = 1;
}

message StockResponse {
  int32 quantity = 1;
}